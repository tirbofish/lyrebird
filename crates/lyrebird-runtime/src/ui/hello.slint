import { VerticalBox } from "std-widgets.slint";
export component Runtime inherits Window {
    in property <image> texture <=> image.source;

    VerticalBox {
        image := Image {
            preferred-width: 640px;
            preferred-height: 640px;
            min-width: 64px;
            min-height: 64px;
            width: 100%;
            //height: 100%;
        }
    }
}